# Agents

This folder holds the package-local agent source of truth for `agent-unity`.

- `policies/` contains the package's own policy fragments and config.
- `skills/` is reserved for future package-local skill sources.
- Built-in renderer templates live in `../templates/`.
